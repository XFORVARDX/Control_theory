clc; clear all

% w=0:0.1:1;
% K=6250
% A=(K*w.^2-10.8*10^8)+1i*(w.^3+86.4*10^8*w);
% plot(real(A),imag(A)), grid on

% Определение переменных
s = tf('s');
K = 650000000;

% Определение переходной функции
A = K*(s+1) / ((10.8*10^8*(s+8)*s^2) + K*(s+1));

% Получение времени и значений переходной функции
t = 0:0.01:300;
[y, t] = step(A, t);

% Построение графика переходной функции
figure;
plot(t, y);

xlabel('t');
ylabel('h(t)');
y=[10.8*10^8 8*10.8*10^8 0.1 0.1];
roots(y)
